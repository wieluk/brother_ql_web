name: Build and Push release tag to GHCR.io

on:
  push:
    branches:
      - '**'
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      build_branch:
        description: 'Build image for current branch (not main/master or tag)'
        required: false
        default: ''
  delete:
    branches:
      - '**'

jobs:
  build-and-push:
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set Docker tags
        id: tags
        run: |
          TAGS=""
          TAGNAME=""
          if [[ "${GITHUB_EVENT_NAME}" == "push" ]]; then
            if [[ "${GITHUB_REF_TYPE}" == "tag" && "${GITHUB_REF_NAME}" =~ ^v && "${GITHUB_REF_PROTECTED}" == "true" ]]; then
              TAGS="ghcr.io/wieluk/brother-ql-web:latest,ghcr.io/wieluk/brother-ql-web:${GITHUB_REF_NAME}"
              TAGNAME="latest"
            elif [[ "${GITHUB_REF_TYPE}" == "branch" ]]; then
              if [[ "${GITHUB_REF_NAME}" == "master" ]]; then
                TAGS="ghcr.io/wieluk/brother-ql-web:latest"
                TAGNAME="latest"
              elif [[ "${GITHUB_REF_NAME}" != "main" ]]; then
                TAGS="ghcr.io/wieluk/brother-ql-web:${GITHUB_REF_NAME}"
                TAGNAME="${GITHUB_REF_NAME}"
              fi
            fi
          elif [[ "${GITHUB_EVENT_NAME}" == "workflow_dispatch" ]]; then
            BRANCH="${GITHUB_REF_NAME}"
            if [[ "$BRANCH" != "master" && "$BRANCH" != "main" && ! "$BRANCH" =~ ^v ]]; then
              TAGS="ghcr.io/wieluk/brother-ql-web:${BRANCH}"
              TAGNAME="$BRANCH"
            elif [[ "$BRANCH" == "master" ]]; then
              TAGS="ghcr.io/wieluk/brother-ql-web:latest"
              TAGNAME="latest"
            fi
          fi
          echo "tags=$TAGS" >> $GITHUB_OUTPUT
          echo "tagname=$TAGNAME" >> $GITHUB_OUTPUT

      - name: Build and push Docker image
        if: steps.tags.outputs.tags != ''
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile
          push: true
          tags: ${{ steps.tags.outputs.tags }}
          platforms: linux/amd64,linux/arm64
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Cleanup old image versions for tag
        if: steps.tags.outputs.tags != ''
        env:
          GHCR_REPO: wieluk/brother-ql-web
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAGNAME: ${{ steps.tags.outputs.tagname }}
        run: |
          # Install gh CLI and jq
          sudo apt-get update && sudo apt-get install -y gh jq
          echo $GH_TOKEN | gh auth login --with-token
          # Get all versions for the tag
          VERSIONS=$(gh api "/orgs/wieluk/packages/container/brother-ql-web/versions" --paginate | jq -r ".[] | select(.metadata.container.tags[]? == \"$TAGNAME\") | .id")
          # Keep only the latest version, delete the rest
          KEEP=$(echo "$VERSIONS" | head -n1)
          DELETE=$(echo "$VERSIONS" | tail -n +2)
          for id in $DELETE; do
            gh api -X DELETE "/orgs/wieluk/packages/container/brother-ql-web/versions/$id"
          done

  cleanup-ghcr:
    if: github.event_name == 'delete' && github.event.ref_type == 'branch'
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    steps:
      - name: Delete GHCR image tag for deleted branch
        env:
          GHCR_REPO: ghcr.io/wieluk/brother-ql-web
          BRANCH: ${{ github.event.ref }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "Deleting GHCR image tag for branch: $BRANCH"
          # Install gh CLI
          sudo apt-get update && sudo apt-get install -y gh
          echo $GH_TOKEN | gh auth login --with-token
          # Delete the tag (ignore errors if not found)
          gh api -X DELETE \
            "/orgs/wieluk/packages/container/brother-ql-web/versions" \
            --paginate \
            | jq -r '.[] | select(.metadata.container.tags[]? == env.BRANCH) | .id' \
            | xargs -r -I{{}} gh api -X DELETE "/orgs/wieluk/packages/container/brother-ql-web/versions/{{}}"
